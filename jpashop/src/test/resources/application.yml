spring:
#이게 다 없어도 됨
#스프링 부트가 기본적으로 별도의 설정이 없으면 메모리 모드로 돌려버리기 때문에
#  datasource:
#    # url을 메모리로 바꾸면 됨 -> 메모리 모드로 동작함
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        # show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace
